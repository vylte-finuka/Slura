# Maraset – manifest SluEnvSys.slul

package:
  name: base***SluEnvSys
  version: 1.0.0
  build: 25072616

metadata:
  target: arm64
  bundle: slul
  attributes:
    SRID: SRID_slura_os_29062617
    Marav: 0.1 build 25072616-dev
    description: |
      Driver Slura OS – Gestionnaire de variables d'environnement SRFS.
      Stocke PRIMARY_VALIDATOR_PRIVKEY, SLURACHAIN_NETWORK et toutes
      les variables système dans SDC:\env\<VAR>.env + SDC:\env\.index
      Injecte les défauts au démarrage (testnet, port 8545/30303).
    slura_min: 1
    slura_max: 5.0

dependencies:
  basecon:
    base***SluraOS***APrevent: 25072616
    base***SluraOS***OEntry:   25072616

resources:
  - path: RAbstractallowing.xml
    type: xml
    description: "Permissions driver SluEnvSys."
