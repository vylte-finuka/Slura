[package]
name = "vuc-core"
version = "0.1.0"
edition = { workspace = true }

[dependencies]
# === Core UEFI ===
uefi = { version = "0.32", features = ["alloc"] }
uefi-services = "0.28"

# === Fixes obligatoires pour UEFI ===
getrandom = { version = "0.2", features = ["custom"] }
hashbrown = { version = "0.14", features = ["ahash"] }   # remplace std::collections

# === Tes crates internes (gardées) ===
vuc-types = { path = "../vuc-types" }
vuc-events = { path = "../vuc-events" }
vuc-common = { path = "../vuc-common" }

# === Dépendances externes allégées (no_std) ===
serde = { workspace = true, default-features = false, features = ["derive"] }
rand = { workspace = true, default-features = false, features = ["std_rng"] }
sha2 = { workspace = true, default-features = false }
md5 = { workspace = true, default-features = false }

# Désactive temporairement les plus lourds (à réactiver plus tard avec scheduler)
# tokio = { workspace = true }
# tracing = { workspace = true }
# futures = { workspace = true }
# chrono = { workspace = true, default-features = false, features = ["alloc"] }  # si vraiment besoin

[lib]
name = "vuc_core"
crate-type = ["staticlib"]
path = "src/lib.rs"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"

[features]
default = ["full"]
full = ["serde/derive"]
